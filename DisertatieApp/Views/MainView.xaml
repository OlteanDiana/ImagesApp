<Window x:Class="DisertatieApp.Views.MainView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:views="clr-namespace:DisertatieApp.Views"
    xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
    xmlns:cmd="clr-namespace:GalaSoft.MvvmLight.Command;assembly=GalaSoft.MvvmLight.Platform"
    Title="Application" 
    Height="600" 
    Width="700"
    DataContext="{Binding Source={StaticResource Locator}, Path=MainVM}">

    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Closing">
            <i:InvokeCommandAction Command="{Binding WindowClosingCommand}" />
        </i:EventTrigger>
    </i:Interaction.Triggers>

    <Grid
        HorizontalAlignment="Stretch"
        VerticalAlignment="Stretch">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Menu Grid.Row="0"
              FontWeight="Bold"
              FontSize="14">
            <MenuItem Header="_Options">
                <MenuItem Header="_Select folder" Command="{Binding OpenFolderBrowserCmd}"/>
                <MenuItem Header="See _movie" Command="{Binding OpenMovieViewCmd}" IsEnabled="{Binding IsMovieButtonEnabled}"/>
            </MenuItem>
        </Menu>

        <views:ThumbnailContainerView Grid.Row="1"/>
    </Grid>
</Window>
